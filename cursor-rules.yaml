rules:
  - Always separate frontend (Next.js) and backend (FastAPI).
  - Use Tailwind for UI styling, keep dashboard clean and minimal.
  - All AI/ML logic must be in /brain folder in Python.
  - Data storage must start with mongo for MVP.
  - APIs should return JSON with clusters, sentiment, and ROI score.
